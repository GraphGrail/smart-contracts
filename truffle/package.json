{
  "name": "gg-smart-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --network local",
    "test": "truffle test",
    "test-local": "truffle test --network local"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "truffle": "^4.1.3"
  },
  "dependencies": {}
}
